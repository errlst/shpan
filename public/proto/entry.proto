// 获取条目信息时返回的数据
syntax = "proto3";

package proto;

message EntryItem {
    uint64 size = 1;
    bool is_dir = 2;
    string path = 3;
    string shared_link = 4;
}

message Entrys { repeated EntryItem entrys = 1; }