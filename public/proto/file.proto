syntax = "proto3";

package proto;

// 上传或下载时的文件元数据
message FileMeta {
    uint64 id = 1;
    uint64 size = 2;
    string path = 3;
    string hash = 4;
};

//
message FileTrunk {
    uint64 id = 1;  // 所属 fileblob 的id
    uint64 idx = 2; // 当前块索引
    string hash = 3;
    bytes data = 4;
};